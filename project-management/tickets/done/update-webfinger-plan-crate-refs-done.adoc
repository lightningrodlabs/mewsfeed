= Update webfinger-plan.md: Replace activitypub_types References
:severity: HIGH

== Summary

link:../webfinger-plan.md[`webfinger-plan.md`] references
`crates/activitypub_types/` throughout. This crate does not exist. The WebFinger
types are actually in
link:../../crates/activitypub-s2s/src/webfinger.rs[`crates/activitypub-s2s/src/webfinger.rs`].

Additionally, the plan names the link struct `JrdLink` while the actual
implementation uses `WebFingerLink` (with a `properties` field that `JrdLink`
lacks).

== Affected Locations

* link:../webfinger-plan.md[`webfinger-plan.md`] lines 105--107 (architecture
  diagram)
* link:../webfinger-plan.md[`webfinger-plan.md`] line 132 (section header)
* link:../webfinger-plan.md[`webfinger-plan.md`] lines 567--573 (Phase 1 types)
* link:../webfinger-plan.md[`webfinger-plan.md`] lines 884--886 (file summary
  table)
* link:../webfinger-plan.md[`webfinger-plan.md`] lines 900--908 (dependency
  declarations)
* All code samples using `JrdLink` should reference `WebFingerLink`

== Acceptance Criteria

* All references to `activitypub_types` are replaced with the correct crate
  name (`activitypub-s2s` for protocol types, `federation_types` for shared
  state).
* `JrdLink` is renamed to `WebFingerLink` in code samples.
* The `WebFingerLink` code sample includes the `properties` field.
* Dependency declarations in the plan match the actual `Cargo.toml` files.
